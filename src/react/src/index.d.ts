import { VirtualList, VirtualListProps, VirtualListHandle } from "./VirtualList";

export = VirtuaReact;
export as namespace VirtuaReact;

declare namespace VirtuaReact {
	// Core

	// Components
	export { VirtualList, type VirtualListProps, type VirtualListHandle };
}
